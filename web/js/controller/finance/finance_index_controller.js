/**
 * 主菜单：仓储管理 -> 订单管理 控制器
 */
app.controller("finance_index_controller", ["$scope", "$rootScope", "_host", "_basic", "_config", "$filter", function ($scope, $rootScope, _host, _basic, _config, $filter) {


    /**
     * 画面初期显示时，用来获取画面必要信息的初期方法。
     */
    $scope.initData = function () {
    };
    $scope.initData();
}]);

